---
# Tenants directory kustomization — the parent KS (cluster-tenants)
# renders this path; the ResourceSet definition is the only resource.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./apps.yaml
